// DEV MODE flag for stubbed native services.
// Set to false when real native implementations are ready.
// When false, native modules (ScreenTime, Notifications) must be available.
export const DEV_MODE = false;
